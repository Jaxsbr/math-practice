# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 5
  phase: foundation
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-MP3] Implement quiz UI — ConfigScreen, QuizScreen with problem display, answer input, feedback, score, difficulty indicator, and localStorage persistence for session and difficulty state"
  consecutive_fails: 0
  completed_stories: [US-MP1, US-MP2, US-MP3, US-MP4]
