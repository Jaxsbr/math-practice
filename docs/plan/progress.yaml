# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 7
  phase: user-profiles
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-10] Wire legacy migration into ProfileScreen (auto-create Player 1 on first load if legacy key exists), add independence test (two profiles have separate progress)"
  consecutive_fails: 0
  completed_stories: [US-09]
