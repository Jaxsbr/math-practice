# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 2
  phase: adventure-map
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-01] Implement challenge data model (challenges.ts), map progress persistence (mapProgress.ts), and new types — 4 paths x 5 nodes, convergence points, difficulty configs, localStorage read/write"
  consecutive_fails: 0
  completed_stories: []
