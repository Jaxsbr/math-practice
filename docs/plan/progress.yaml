# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 5
  phase: user-profiles
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-09] Integrate ProfileScreen into App.tsx as initial view, update mapProgress to accept profileId for per-profile scoping, add legacy migration"
  consecutive_fails: 0
  completed_stories: [US-09]
