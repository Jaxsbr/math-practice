# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 3
  phase: foundation
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-MP2] Implement generateProblem pure function in src/lib/generator.ts with configurable operations and range, plus comprehensive unit tests"
  consecutive_fails: 0
  completed_stories: [US-MP1, US-MP2]
