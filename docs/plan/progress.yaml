# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 1
  phase: foundation
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: null
  next_task: "[US-MP1] Scaffold React + Vite + TypeScript project with Vitest, configure base path, add smoke test, and create GitHub Pages deploy workflow"
  consecutive_fails: 0
  completed_stories: [US-MP1]
