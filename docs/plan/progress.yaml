# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 9
    builds: [9, 11, 12]

state:
  task_number: 12
  phase: reward-magic
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-16] Add ambient soundscape — continuous filtered noise on MapScreen (louder) and QuizScreen (softer), cross-fade on screen transitions, mute-aware, cleanup on unmount"
  consecutive_fails: 0
  completed_stories: [US-12, US-13, US-14, US-15]
