# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 3
  phase: adventure-map
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-01] Implement MapScreen component — SVG/CSS adventure map with 4 winding paths, challenge nodes in 3 states (locked/unlocked/completed), convergence nodes visually distinct, wire as App entry point replacing ConfigScreen"
  consecutive_fails: 0
  completed_stories: []
