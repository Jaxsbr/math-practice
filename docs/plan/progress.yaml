# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 3
  phase: user-profiles
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-09] Implement ProfileScreen.tsx + ProfileScreen.css — avatar selection, profile cards, creation flow, name validation, adventure-themed parchment styling consistent with MapScreen"
  consecutive_fails: 0
  completed_stories: []
