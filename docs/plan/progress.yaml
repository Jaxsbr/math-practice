# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 8
  phase: number-sense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-07] Implement milestone problem generation — filter operations to only completed lanes at the milestone's tier, with test"
  consecutive_fails: 0
  completed_stories: [US-05, US-06, US-07]
