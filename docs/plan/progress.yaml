# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npx tsc --noEmit && npm run lint && npm test"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 6
  phase: number-sense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-07] Refactor challenges.ts: add R1-R5 + N1-N5 nodes, replace C1/C2 with MS1/MS2 milestones, add requiredCount/MILESTONE_REQUIRED, update tier-3 prerequisites"
  consecutive_fails: 0
  completed_stories: [US-05, US-06]
